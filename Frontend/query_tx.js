const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://postgres:admin@localhost:5432/share-money' }); client.connect().then(() => client.query('SELECT wallet_id, SUM(CASE WHEN type = \'INCOME\' THEN amount ELSE 0 END) AS total_income, SUM(CASE WHEN type = \'EXPENSE\' THEN amount ELSE 0 END) AS total_expense FROM transactions GROUP BY wallet_id HAVING SUM(CASE WHEN type = \'INCOME\' THEN amount ELSE 0 END) = 12500000 AND SUM(CASE WHEN type = \'EXPENSE\' THEN amount ELSE 0 END) = 7850000').then(res => { console.log(res.rows); client.end(); })).catch(console.error);
